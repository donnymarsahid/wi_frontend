import { Work_Sans } from "next/font/google";
// import { Poppins } from "next/font/google";

export const poppins = Work_Sans({
  // export const poppins = Poppins({
  subsets: ["latin"],
  weight: ["400", "500", "600", "700", "800"],
  variable: "--font-poppins",
});
